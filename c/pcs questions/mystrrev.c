#include<stdio.h>
#include<stdlib.h>
void mystrrev(char arr[]){
    int l=0,i=0;
    while(arr[i]!=0){
        i++;
    }
    while(l<i){
        arr
    }
}
int main()
{
    char arr[50];
    mystrrev(arr);
    puts(arr);
    return 0;
}